import { Button } from '@nextui-org/react';

export const CreateOrgSection = () => {
  console.log('TODO');

  return (
    <Button size="sm" className="font-bold">
      Create Organization
    </Button>
  );
};
